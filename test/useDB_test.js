/**
 * Test case for useDB.
 * Runs with mocha.
 */
'use strict'

const useDB = require('../lib/helpers/useDB.js')
const assert = require('assert')

describe('use-d-b', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Use d b', async () => {

  })
})

/* global describe, before, after, it */
